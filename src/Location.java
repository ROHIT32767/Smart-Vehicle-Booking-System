package src;

public interface Location {
    /**
     * The function getCurrentLocation returns the current location as a string.
     * 
     * @return The method getCurrentLocation() is returning a String.
     */
    public String getCurrentLocation();
}
